// Fix #4 — Tab is now registered via api.addProfileTab in setup-user-publications.js.
// Delete this file from the container; the empty template below is a no-op fallback
// in case the file cannot be removed before the next rebuild.
<template></template>
